const NotDataMessage = ({ message}) => {
     return(
        <div className=" text-center w-full p-4 rounded-full bg-grey/50 mt-4 text-xl">
            {
                message
            }
        </div>
     )
}

export default NotDataMessage;